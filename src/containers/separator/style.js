import { StyleSheet } from 'react-native';
import { COLOR, DIMENSION } from "@styles/constants";

const styles = StyleSheet.create({
  container: {
    flex: 1,
    maxHeight: DIMENSION.HAIRLINE,
    backgroundColor: COLOR.BLACK
  }
});

export default styles;
